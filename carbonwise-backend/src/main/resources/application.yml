server:
  port: 8080

spring:
  application:
    name: carbonwise-backend

  datasource:
    url: jdbc:h2:mem:carbonwisedb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${JWT_SECRET:carbonwise-secret-key-change-in-production}
  expiration: 86400000
  refresh-expiration: 604800000

mqtt:
  broker: tcp://localhost:1883
  client-id: carbonwise-backend
  topic-prefix: carbonwise/

ai:
  server-url: http://localhost:5000

logging:
  level:
    com.carbonwise: DEBUG
    org.springframework.security: DEBUG